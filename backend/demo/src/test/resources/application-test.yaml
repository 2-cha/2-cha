spring:
  datasource:
    url: jdbc:h2:mem:test
    driver-class-name: org.h2.Driver
    username: sa
  jpa:
    hibernate:
      ddl-auto: create
cors.origins:
  - /**
jwt.access-key: test
jwt.refresh-key: test
jwt.issuer: http://2cha.test
redis.host: localhost
redis.port: 6379